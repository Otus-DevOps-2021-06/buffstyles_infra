---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    #name: virtualbox
    name: vmware_desktop
lint:
  name: yamllint
platforms:
  - name: instance
    #box: ubuntu/xenial64
    box: hashicorp/bionic64
provisioner:
  name: ansible
  lint:
    name: ansible-lint
verifier:
  name: testinfra
  lint:
    name: flake8
